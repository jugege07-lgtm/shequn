# 数据大屏重设计方案 · 聚格软件 · 用户大厅展板

> 设计目标：在用户接待大厅（典型视距 3-8m）长期滚动展示运营总览，传达"专业、科技、活跃、值得信赖"的品牌气质。
> 设计稿基准：1920×1080，向下兼容 2K/4K，向上可拼屏。

---

## 一、数据接入（与现有管理端接口对齐）

### 1.1 一期接入（已可用，复用现有 `/api/admin/big-screen`）

| 数据模块 | 后端字段 | 刷新策略 | 异常告警阈值 |
|---|---|---|---|
| 总用户数 / VIP 数 / 活动数 / 商机数 / 商品数 / 订单数 | `overview.*` | 30s | 较 1h 前下降 ≥20% 红色脉冲 |
| 今日订单 / 今日营收 | `overview.todayOrders/todayRevenue` | 30s | 营收为 0 但时段≥10:00 黄色告警 |
| 用户增长 14 天趋势 | `userGrowthTrend[]` | 5min | 连续 3 日下滑 橙色提示 |
| 用户来源分布 | `userSource[]` | 1h | — |
| 用户活跃度（VIP 等级） | `userActivity[]` | 1h | — |
| 订单营收 14 天趋势 | `orderRevenueTrend[]` | 5min | — |
| 省份分布 | `provinceDistribution[]` | 1h | — |
| 活动类型分布 | `activityTypeDistribution[]` | 1h | — |
| 待审核数量 | `/admin/dashboard → pendingActivityCount/BusinessCount` | 60s | 任一 > 0 显示黄点 |

### 1.2 二期扩展（需后端新增或埋点）

- 实时在线人数（WebSocket / Socket.IO，预计后续接入）
- 大厅屏设备心跳（前端每 10s 上报一次运行状态）
- 当日活动签到流水（按活动 ID 订阅）
- 平台消息广播条（管理员可主动推送"欢迎词/公告"至大屏底部滚动条）

### 1.3 自动刷新与异常

- WebSocket 通道兜底 + 轮询 30s 双保险
- 失败重试 3 次，指数退避
- 接口 5xx 持续 1min → 顶部"系统状态"变红 + 触发告警音（可静音）
- 数据源为 0 时显示"暂无人入座"等友好文案，避免空白

---

## 二、视觉风格

### 2.1 设计语言：**Cyber Luxury（赛博轻奢）**

- **主色**：深空蓝渐变 `#050a1f → #0a1840 → #0e2552`
- **主强调色**：霓虹青 `#00e5ff`、电光紫 `#7c5cff`
- **次强调色**：极光绿 `#10ffb3`（增长）、落日红 `#ff5577`（告警）
- **背景层**：
  - 第 1 层：径向渐变 + 暗角
  - 第 2 层：动态粒子（≤40 个，低速漂浮）
  - 第 3 层：等距网格 + 扫描线
- **边框**：1px `rgba(0,229,255,.35)` + 外发光 `box-shadow: 0 0 12px rgba(0,229,255,.25)`
- **角装饰**：四角的"切角"装饰条（CSS clip-path）营造 HUD 感
- **字体**：
  - 数字主字体 `Orbitron` / `DIN Alternate`（粗体，远距离可读）
  - 中文标题 `HarmonyOS Sans SC` / `思源黑体`
  - 数据标签 `JetBrains Mono`（等宽，统一表格感）
- **图标**：`Phosphor Icons` 线性 / `lucide`，统一 1.5px 描边

### 2.2 远距离可读性

- 核心 KPI 数字 ≥ 72px，副指标 ≥ 32px，标签 ≥ 18px
- 标题与数字对比度 ≥ 7:1（WCAG AAA）
- 避免过细描边（≥1px），避免低对比灰阶文字

---

## 三、大厅场景适配

| 场景 | 适配策略 |
|---|---|
| 远视距 5m+ | 关键数字 ≥ 80px；面板内边距 24px；减少细线条 |
| 多屏拼接 | 1920×1080 基准，多屏 `transform: scale()` 等比缩放；关键模块放中心区，避免边缘被裁切 |
| 7×24 长期滚动 | 避免纯白高亮；夜间模式自动降低饱和度（22:00-07:00 蓝紫色调稍暗） |
| 非专业观众 | 大数字 + 简短中文标签（4 字内）；辅以图标/颜色传达趋势；轮播图自动播放并配文字解读 |
| 紧急信息 | 顶部状态条支持"公告横幅"插队，滚动 1 次后自动收回 |

---

## 四、模块规划（标准 1920×1080 网格）

### 4.1 顶部 80px —— 全局信息带
- 左侧：实时时间（HH:MM:SS，72px Orbitron）、日期、星期
- 中部：项目 LOGO + 标语"社群名片 · 实时运营态势"
- 右侧：全屏按钮、当前刷新倒计时、API 健康度小灯（绿/红）

### 4.2 上半 320px —— 6 个核心 KPI
- 总用户数 / VIP 用户 / 今日订单 / 今日营收 / 活动总数 / 商机总数
- 每个 KPI：图标 + 大数字 + 中文标签 + 较昨日 +% 趋势小条
- 数字滚动动画 1.2s

### 4.3 中部三栏（约 540px）

| 左栏（28%） | 中栏（44%） | 右栏（28%） |
|---|---|---|
| 用户增长 14 天折线 + 渐变面积 | 中心 3D 环形地球（基于 ECharts GeoJSON） | 实时排行榜（活动报名 TOP5 / 商机热度 TOP5 切换） |
| 用户来源环形图 | 省份柱状排名 | 待审核活动 / 商机（点击进入管理端） |
| 用户活跃度柱状 | | |

### 4.4 下半 160px —— 信息流
- 左：管理员最新公告（最多 3 条，10s 轮换）
- 中：活动签到流水（"23:14 张三 报名了《AI 产品私享会》"）
- 右：系统消息 + 二维码（社群入口、APP 下载）

### 4.5 动效节奏
- 数字滚动：1.2s 缓动（cubic-bezier 0.2, 0.8, 0.2, 1）
- 图表刷新：淡入 400ms，不做整图重绘
- 轮播：每 8s 切换，缓动 600ms
- 粒子：永远 0.3-0.6 速率漂浮，不闪烁
- 告警：脉冲 1s/次（不超过 3 次后保持常亮）
- **视觉疲劳控制**：所有动画 ≤ 2.5s；任意区域同时最多 2 个动画进行

---

## 五、响应与交互

### 5.1 分辨率适配

```
设计基准：1920 × 1080
支持范围：1280×720 ~ 7680×2160（4 屏拼接）
实现：CSS transform: scale() 基于父容器实际尺寸
字体：使用 vw/vh 单位做等比缩放（基准 1rem = 1vh）
```

### 5.2 拼屏模式
- 默认 1 屏
- 支持横拼 2/3/4 屏：通过 URL `?cols=2&rows=1` 控制
- 拼接时自动隐藏重复的时间/标题，主屏承担全局信息

### 5.3 交互（轻交互，非主诉求）
- **点击 KPI 卡片** → 弹出该指标的趋势小图（弹层 1.5s 后自动收起）
- **点击省份柱** → 中心地图高亮该省
- **点击活动/商机** → 跳转管理端对应详情
- **右键** → 弹出配置面板（轮播间隔、刷新频率、是否静音）
- **键盘快捷键**：`F` 全屏 / `R` 立即刷新 / `1-9` 切换页面主题
- 触摸：≥5s 无操作 → 恢复轮播；点击立即响应

### 5.4 主题
- 蓝紫主（默认）
- 极光绿（环保/数据节庆）
- 落日橙（周年庆/活动日）
- 极简白（部分 B 端接待场景）

---

## 六、交付物

1. 本设计文档 `bigscreen-design.md`
2. **可独立打开的 HTML 原型** `bigscreen-v2.html`（自包含，无构建步骤）
   - 数据通过 `fetch('http://localhost:3000/api/admin/big-screen')` 拉取，token 写死在 `localStorage` 或 URL 参数
   - 真实数据 + 视觉验证；离线/未登录时自动回退 mock 数据，确保设计评审不受接口影响
3. Vue 集成方案：替换 `admin/src/views/bigscreen/Index.vue`，样式与组件按本设计重构，1-2 个工作日完成

---

## 七、风险与备注

- 中国地图 GeoJSON 远程依赖 `https://geo.datav.aliyun.com/areas_v3/bound/100000_full.json`，部署到内网时需离线打包。
- ECharts 5 默认不支持霓虹边框，需自定义 series.itemStyle / lineStyle.shadowBlur。
- 1920×1080 物理屏 → 大厅硬件建议 4K 屏（3840×2160）做 2x 渲染更锐利。
- 长时间运行建议每 6h 自动 `location.reload()` 一次以释放内存。
