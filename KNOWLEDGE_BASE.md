# 🕳️ 踩坑日志 (Knowledge Base)

> 记录所有踩过的坑、解决方案、技术决策、常见错误  
> 原则：前人踩过的坑，后人可以直接绕过去

---

## 🔧 环境与工具坑

### macOS 文件系统权限
- **问题**：`~/Documents/Workspace` 等系统目录可能受 SIP/沙盒限制，无法直接写入
- **解决**：使用 `~/Documents/代码/codex/Workspace` 或其他明确可写的目录
- **检查方法**：先 `touch test_file` 验证写入权限
- **标签**：`#工具` `#环境` `#macOS`

### zsh vs bash
- **注意**：确认用户使用 zsh 还是 bash，脚本语法有差异
- **变量引用**：zsh 中 `$VAR` 和 `${VAR}` 行为基本一致
- **通配符**：zsh 默认开启 `GLOB_STAR`，`**` 匹配递归目录
- **标签**：`#工具` `#shell`

---

## 💻 开发通用坑

### 依赖安装失败
- **常见原因**：网络问题、镜像源未配置、Node/Python 版本不匹配
- **预防**：项目初始化时先确认包管理器可用，配置国内镜像源
- **标签**：`#踩坑` `#工具`

### 路径问题（相对 vs 绝对）
- **坑**：在多目录结构中，相对路径容易因 cwd 变化而失效
- **解决**：关键路径使用绝对路径或基于项目根目录的相对路径
- **标签**：`#踩坑` `#规范`

### 编码问题
- **坑**：文件编码不一致导致解析失败（UTF-8 vs GBK）
- **预防**：统一使用 UTF-8 编码
- **标签**：`#踩坑` `#规范`

---

## 📐 技术决策日志

> 记录重要的技术选型及其理由，供后续项目参考

<!--
示例格式：

## 2026-06-28
### [技术名] 决策标题
- **背景**：为什么需要做这个决策
- **选项**：对比了哪些方案
- **选择**：最终选了哪个
- **理由**：为什么选这个
- **代价**：放弃了什么
-->

---

## 🚨 常见错误速查表

| 错误现象 | 可能原因 | 解决方案 |
|---------|---------|---------|
| `Operation not permitted` | macOS 沙盒限制 | 换到可写目录 |
| `command not found` | 工具未安装或 PATH 不对 | `which <cmd>` 检查 |
| 端口被占用 | 旧进程未释放 | `lsof -ti:PORT | xargs kill` |
| 模块找不到 | 未安装或路径不对 | 检查 `package.json` / `requirements.txt` |
| 编码乱码 | 文件非 UTF-8 | `iconv -f GBK -t UTF-8 input -o output` |

---

## 📝 问题记录区

> 遇到新问题？在这里记录，格式如下：

<!--
示例格式：

## 2026-06-28 14:30
### [#踩坑] 问题标题
- **场景**：在做什么的时候遇到的问题
- **错误信息**：完整的报错内容
- **排查过程**：尝试了哪些方法
- **根本原因**：到底是什么导致的
- **解决方案**：最终怎么解决的
- **预防措施**：下次如何避免
- **影响范围**：哪些项目可能受影响
-->

---

*本文件应随项目推进持续更新。每次踩坑后花 1 分钟记录，就是对未来自己最大的投资。*
